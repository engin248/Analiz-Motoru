name: Frontend Deploy

on:
  push:
    branches:
      - main
    # paths kısmını SİLDİK, artık her değişiklikte çalışacak

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            set -e
            echo "🔌 Sunucuya bağlanılıyor..."
            
            # Sunucudaki klasör yolunuz (Burası doğru, değişmesin)
            cd /var/www/backend/chatbot_frontend
            
            echo "📥 Güncel kodlar çekiliyor..."
            git pull origin main
            
            echo "📦 Paketler yükleniyor..."
            npm install
            
            echo "🏗️ Build alınıyor..."
            npm run build
            
            echo "🚀 PM2 yeniden başlatılıyor..."
            pm2 restart lumora-frontend
            
            echo "✅ Frontend Başarıyla Güncellendi!"
